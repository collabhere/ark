version: 2.1

jobs:
  build_linux:
    docker:
      - image: node:16
    steps:
      - checkout
      - run:
          name: "Install dependencies"
          command: "npm ci"
      - run:
          name: "Build electron & react"
          command: "npm run build"
      - run:
          name: "Build distributable"
          command: "npm run dist:linux"
      - run:
          name: "Move necessary build output"
          command: "mkdir -p /tmp/release && mv ./release/*.AppImage /tmp/release"
      - store_artifacts:
          path: /tmp/release

workflows:
  main:
    jobs:
      - build_linux
